{
  "name": "empilha",
  "version": "1.0.0",
  "description": "Empilha! - jogo de empilhar blocos",
  "scripts": {
    "build": "esbuild src/native.js --bundle --minify --format=iife --target=es2019 --outfile=www/native.js",
    "sync": "npm run build && npx cap sync android"
  },
  "dependencies": {
    "@capacitor-community/admob": "^8.1.0",
    "@capacitor/android": "^8.5.2",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.5.2",
    "@capgo/native-purchases": "^8.8.1"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.5.2",
    "@fontsource/bungee": "^5.3.0",
    "@fontsource/nunito": "^5.3.0",
    "esbuild": "^0.28.2"
  },
  "private": true
}
